#include "main.h"
/**
* _strlen - function about ckeck for a digit
* @s: is pointer variable
* Return: i is correct
*/

int _strlen(char *s)

{
	int i;

	for (i = 0; (s[i] != '\0'); ++i)
	{

	}
		return (i);
}
