#include "main.h"
/**
* _isdigit - function about ckeck for a digit
* @c: is compare the enter
* Return: 1 if yes digit 0 other
*/

int _isdigit(int c)

{

	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
